///
/// HybridNitroFusedLocationSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `LocationData` to properly resolve imports.
namespace margelo::nitro::nitrofusedlocation { struct LocationData; }

#include "LocationData.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <functional>

namespace margelo::nitro::nitrofusedlocation {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroFusedLocation`
   * Inherit this class to create instances of `HybridNitroFusedLocationSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroFusedLocation: public HybridNitroFusedLocationSpec {
   * public:
   *   HybridNitroFusedLocation(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroFusedLocationSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroFusedLocationSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroFusedLocationSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<LocationData>> getCurrentLocation() = 0;
      virtual std::shared_ptr<Promise<std::string>> watchPosition(const std::function<void(const LocationData& /* data */)>& callback) = 0;
      virtual std::shared_ptr<Promise<void>> clearWatch(const std::string& watchId) = 0;
      virtual std::shared_ptr<Promise<bool>> isGpsEnabled() = 0;
      virtual std::shared_ptr<Promise<void>> resetDistance() = 0;
      virtual std::shared_ptr<Promise<void>> setGeofence(double lat, double lng, double radius) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroFusedLocation";
  };

} // namespace margelo::nitro::nitrofusedlocation
